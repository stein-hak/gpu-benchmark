# Docker Image Configuration
# Copy this file to .env and configure

# Use pre-built image from Docker Hub (recommended)
IMAGE_NAME=steinhak/gpu-benchmark
IMAGE_TAG=latest

# Build mode: 'build' or 'pull'
# pull = Pull from Docker Hub (faster, recommended)
# build = Build image locally (slower, for development)
BUILD_MODE=pull
